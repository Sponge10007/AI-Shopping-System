# Database

`migrations/001_initial_schema.sql` defines the first PostgreSQL schema for the course prototype.

The current backend scaffold does not connect repositories yet. When persistence starts, add one migration per feature and keep API DTOs separate from database entities.

